# This override file should be used when running automated tests so
# that test output is saved to the host

services:
  fcp-fd-file-processor:
    build:
      target: development
    image: fcp-fd-file-processor-development
    container_name: fcp-fd-file-processor-test
    command: npm run test
    volumes:
      - ./app:/home/node/app
      - ./test:/home/node/test
      - ./test-output:/home/node/test-output
      - ./jest.config.js:/home/node/jest.config.js
      - ./package.json:/home/node/package.json
    environment:
      NODE_OPTIONS: "$NODE_OPTIONS --experimental-vm-modules"
      AZURITE_ENABLED: true
      AZURITE_HOST: "http://fcp-fd-file-processor-azurite-test"
      AZURITE_BLOB_PORT: 10000
      AV_SCAN_POLLING_INTERVAL: 50
      USE_AZURITE: true
      AZURITE_ACCESS_KEY: "test"
      DMZ_STORAGE_ACCOUNT_NAME: "dmz"
      MAL_STORAGE_ACCOUNT_NAME: "mal"
    
  fcp-fd-file-processor-azurite:
    container_name: fcp-fd-file-processor-azurite-test
    environment:
      AZURITE_ACCOUNTS: >
        dmz:test;cln:test;mal:test
